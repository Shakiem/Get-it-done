<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:actionBarStyle">@style/MyActionBar</item>
        <!--<item name="actionButtonStyle">@style/MyActionButtonStyle</item>-->
        <item name="android:textColorHighlight">@color/translucent_app_blue</item>
        <item name="android:textColor">@color/app_dark_blue</item>
        <item name="android:activatedBackgroundIndicator">@drawable/list_selector</item>

        <!-- Support library compatibility -->
        <item name="actionBarStyle">@style/MyActionBar</item>

    </style>

    <!-- ActionBar styles -->
    <style name="MyActionBar" parent="@style/Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <item name="android:background">@drawable/action_bar</item>
        <!-- Support library compatibility -->
        <item name="background">@drawable/action_bar</item>
    </style>

    <style name="MyActionBarTheme" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:textColorPrimary">@color/app_dark_blue</item>
    </style>

    <!--<style name="MyActionButtonStyle" parent="@style/Widget.AppCompat.Light.ActionBar.Solid.Inverse">-->
        <!--<item name="android:minWidth">80dip</item>-->
        <!--<item name="android:padding">0dip</item>-->
    <!--</style>-->

    <style name="NoActionBar" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:actionBarStyle">@style/MyActionBar</item>
        <item name="android:textColorHighlight">@color/translucent_app_blue</item>
        <item name="android:activatedBackgroundIndicator">@drawable/list_selector</item>
        <!--<item name="android:statusBarColor">#659D32</item>--> <!-- requires api 21 min-->

        <!-- Support library compatibility -->
        <item name="actionBarStyle">@style/MyActionBar</item>

    </style>

</resources>
